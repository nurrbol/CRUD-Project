CREATE TABLE IF NOT EXISTS cinemas (
                                       id SERIAL PRIMARY KEY,
                                       name TEXT NOT NULL,
                                       location TEXT NOT NULL
);
